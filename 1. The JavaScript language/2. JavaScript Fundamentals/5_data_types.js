
let name = "Ilya";

alert( `hello ${1}` ); // the expression is a number 1

alert( `hello ${"name"}` ); // the expression is a string "name"

alert( `hello ${name}` ); // the expression is a variable, embed it